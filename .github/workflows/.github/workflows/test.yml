name: Test Workflow

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Testando o GitHub Actions
        run: echo "GitHub Actions está funcionando!"
