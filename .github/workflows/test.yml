name: Test Workflow

on:
    push:
      branches:
        - master
    pull_request:
      branches:
        - master

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Testando GitHub Actions
        run: echo "GitHub Actions está funcionando!"
